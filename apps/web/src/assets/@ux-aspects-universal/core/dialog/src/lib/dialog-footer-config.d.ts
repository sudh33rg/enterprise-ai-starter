import { DialogDivider } from './dialog-divider';
import { DialogFooterAlignment } from './dialog-footer-alignment';
/** Dialog footer component configuration. */
export interface DialogFooterConfig {
    align: DialogFooterAlignment;
    divider: DialogDivider;
}
