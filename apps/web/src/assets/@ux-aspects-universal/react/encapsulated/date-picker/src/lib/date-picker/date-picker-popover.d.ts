import { PopoverProps } from '@ux-aspects-universal/react/popover';
import * as React from 'react';
import './date-picker-popover.scss';
export declare function DatePickerPopover({ children, ...props }: PopoverProps): React.ReactNode;
