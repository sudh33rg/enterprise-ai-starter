import { ReactElement, ReactNode } from 'react';
interface PaginationDropdownProps {
    dropdown?: ReactElement;
}
export declare function PaginationDropdown({ dropdown }: PaginationDropdownProps): ReactNode;
export {};
