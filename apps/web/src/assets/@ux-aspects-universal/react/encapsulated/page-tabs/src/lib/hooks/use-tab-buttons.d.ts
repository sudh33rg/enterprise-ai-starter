import * as React from 'react';
import { PageTabButtonProps } from '../page-tab-button';
export type UseTabButtons = React.ReactElement<PageTabButtonProps>[];
export declare function useTabButtons(children: React.ReactNode): UseTabButtons;
