import * as React from 'react';
import { FC } from 'react';
import './masthead-version.scss';
export type MastheadVersionProps = {
    children?: React.ReactNode;
};
export declare const MastheadVersion: FC<MastheadVersionProps>;
