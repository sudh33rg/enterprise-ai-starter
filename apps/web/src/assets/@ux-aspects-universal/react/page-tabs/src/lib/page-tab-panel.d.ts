import * as React from 'react';
interface PageTabPanelProps {
    id: string;
    children: React.ReactNode;
}
export declare function PageTabPanel({ id, children }: PageTabPanelProps): React.ReactNode;
export {};
