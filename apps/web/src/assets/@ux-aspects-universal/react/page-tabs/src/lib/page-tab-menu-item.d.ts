import * as React from 'react';
import { PageTabProps } from './page-tab';
interface PageTabMenuItemProps {
    tab: React.ReactElement<PageTabProps>;
}
export declare function PageTabMenuItem({ tab }: PageTabMenuItemProps): React.JSX.Element;
export {};
