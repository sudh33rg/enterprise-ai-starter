{
  "name": "enterprise-ai-starter",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "apps/api",
    "apps/web"
  ],
  "engines": {
    "node": ">=18.18.0"
  },
  "scripts": {
    "build": "npm -ws run build",
    "dev": "npm -ws run dev",
    "start": "npm -ws run start",
    "postinstall": "npm -ws run postinstall"
  }
}